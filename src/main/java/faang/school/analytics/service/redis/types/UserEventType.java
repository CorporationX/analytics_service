package faang.school.analytics.service.redis.types;

// TODO: Think how to combine with EventType enum to not repeat each time
public enum UserEventType {
    CREATED,
    DELETED,
    UPDATED,
    DEACTIVATED,
}
